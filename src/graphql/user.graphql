query user {
    user {
        id
        wechat
        nickname
        weibo
        googleId
        githubId
        nickname
        avatar
        bgUrl
        email
        signature
        intro
        gender
        createTime
    }
}

mutation updateUser($updateFields: UserInput!) {
    updateUser(updateFields: $updateFields)
}